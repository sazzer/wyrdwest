refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console

root:
  level: debug
  appenders:
    - stdout

loggers:
  tokio_reactor:
    level: info
  actix_net::server::worker:
    level: warn
  actix_web::server::h1:
    level: info
